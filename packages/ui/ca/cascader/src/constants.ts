import type { CascadeFieldName } from './types';

export const DEFAULT_FIELD_NAMES: CascadeFieldName = {
  label: 'label',
  value: 'value',
  children: 'children',
  disabled: 'disabled',
};

export const DEFAULT_SPLIT_CHAR = ' / ';